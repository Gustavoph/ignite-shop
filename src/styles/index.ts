import { createStitches } from "@stitches/react"

export const {
  config,
  styled,
  globalCss,
  keyframes,
  theme,
  css,
  getCssText,
  createTheme
} = createStitches({
  theme: {
    colors: {
      rocketseat: '#8257e6'
    }
  }
})